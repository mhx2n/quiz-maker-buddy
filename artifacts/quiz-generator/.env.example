# Leave empty when the API is served from the same origin (or use the dev proxy).
# Set this when the frontend is hosted separately (Vercel/Cloudflare) from the API (Render):
# VITE_API_URL=https://your-api.onrender.com
VITE_API_URL=
